<body>
  <header>
    <h1>
      User Profile
    </h1>
  </header>
  <p>About {{username}}</p>
  <p>
    {{#if bio}}
      {{bio}}
    {{else}}
      No bio available.
    {{/if}}
  </p>
  <h2>Preferences</h2>
  {{#if favoriteGameName}}
    <p>Favorite game: {{favoriteGameName}}</p>
  {{else}}
    <p>No favorite game</p>
  {{/if}}
  {{#if leastFavoriteGameName}}
    <p>Least favorite game: {{leastFavoriteGameName}}</p>
  {{else}}
    <p>No least favorite game</p>
  {{/if}}
  <h2>Friends</h2>
  <ul>
    {{#if friends}}
      {{#each friends}}
        <li>
          <a href="/profile/{{id}}">{{username}}</a>
        </li>
      {{/each}}
    {{else}}
      <p>I have no friends</p>
    {{/if}}
  </ul>
  {{#if id}}
    {{#if pageOwned}}
      <a href="/profile/update/bio">Click here to update your bio!</a>
    {{else}}
      {{#if friended}}
        <p>You are friends!</p>
      {{else}}
        <form method="POST" action="/profile/add/{{pageId}}" id="addFriend">
          <button type="submit">Add Friend</button>
        </form>
      {{/if}}
    {{/if}}
  {{/if}}
</body>