<article>
    {{#if reviews}}
        <h2>{{username}}'s Reviews</h2>
        <ul>
            {{#each reviews}}
                <li>
                    <h4>Game: {{self.game}}</h4>
                    <p>Review: {{self.review}}</p>
                    <p>Rating: {{self.rating}}/5</p>
                </li>
            {{/each}}
        </ul>
    {{else}}
        <p>{{username}} has no reviews</p>
    {{/if}}
</article>